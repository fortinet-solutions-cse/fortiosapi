RIEMANN_CONFIG_PATH="/etc/riemann"
LANGOHR_HOME="/opt/lib"
EXTRA_CLASSPATH="/opt/lib/langohr.jar"

RABBITMQ_HOST="{{ ctx.instance.host_ip }}"
MANAGEMENT_IP="{{ ctx.instance.host_ip }}"

RABBITMQ_HOST="{{ ctx.instance.runtime_properties.rabbitmq_endpoint_ip }}"
RABBITMQ_USER="{{ ctx.instance.runtime_properties.rabbitmq_username }}"
RABBITMQ_PASS="{{ ctx.instance.runtime_properties.rabbitmq_password }}"
